<div class="modal fade", id="signup_id">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
        <h4 class="modal-title">Sign Up</h4>
      </div>
      <div class="modal-body">
        <p><ul>Save time by using your Facebook or Twitter account to Sign In.</ul>
        <ul>We'll only import your email, photo. We never post unless you ask us to.</ul></p>
        <%= link_to("Facebook",user_omniauth_authorize_path(:facebook), class: "btn btn-primary") %>
        <%= link_to("Twitter",user_omniauth_authorize_path(:twitter), class: "btn btn-info") %>
      </div>
      <div class="modal-footer">
        <%= link_to("Close",'#', class: "btn btn-warning", data: {dismiss: "modal"}) %>
       <%= link_to("Create an Account",new_user_registration_path, class: "btn btn-success") %>
      </div>
</div>
